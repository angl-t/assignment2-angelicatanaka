<ul class="list-unstyled">
    <% users.forEach(user => { %>
        <li>
            <%= user.name %> (<%= user.user_type %>)
            <a class="btn btn-danger mt-3"  href="/admin/promote/<%= user.email %>">Promote to Admin</a>
            <a class="btn btn-success mt-3"  href="/admin/demote/<%= user.email %>">Demote to User</a>
        </li>
    <% }) %>
</ul>
