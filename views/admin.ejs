<%- include("./templates/header") %>
<div class="container">
    <% if (users.length === 0) { %>
        <p>No users found.</p>
    <% } else { %>
        <%- include("./templates/users") %>
    <% } %>
</div>
<%- include("./templates/footer") %>